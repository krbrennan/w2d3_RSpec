class Hand




  # Royal flush <0.001%


  # Straight flush (not including royal flush) <0.002%
    # five sequentially


  # Four of a kind 0.02%
  # Full house 0.14%
  # Flush (excluding royal flush and straight flush) 0.20%
  # Straight (excluding royal flush and straight flush) 0.39%
  # Three of a kind 2.11%
  # Two pair 4.75%
  # One pair 42.30%
  # No pair / High card 50.10%
  def self.sort_hand(hand)
    hand.keys.sort!
    #UNFINISHED
    # case hand
    #
    # when royal_flush(hand)
    #
    # when straight_flush(hand)
    # when four_of_a_kind(hand)
    # when full_house(hand)
    # when flush(hand)
    # when straight(hand)
    # when three_of_a_kind(hand)
    # when two_pair(hand)
    # when pair(hand)
    # when high_card(hand)
    # end
  end

end
